var React = require('react'),
    ReactDOM = require('react-dom');

var TEST = React.createClass({
    render: function(){
        return(
            <h1>哈哈</h1>
        )
    }
})

module.exports = TEST;
